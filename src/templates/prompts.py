"""
Sheetling 用の LLM プロンプト定義集。
マルチステップ・パイプライン方式に基づき、各ステップの役割を定義します。
"""

CHUNKING_PROMPT = """あなたはPDFから抽出したデータを分析するアシスタントです。
入力データ（Markdownテキストと座標情報）を読み取り、論理的なセクションや表の塊（チャンク）ごとに分割してください。
各チャンクには、その種類（text, table, headingなど）と、大まかなページ内の位置情報を含めてください。

**【重要】 `content` には、その範囲に含まれる元のテキスト内容を一切省略・要約せずに、そのまま含めてください。**
テーブルの場合も「〜の一覧」のようにまとめず、Markdown 形式の表データをそのまま保持してください。

ページ情報:
{page_info}

入力データ:
{input_data}

出力形式はJSON配列のみとしてください。
"""

STRUCTURE_ALIGNMENT_PROMPT = """あなたは抽出されたテキストの論理構造を補正するアシスタントです。
STEP 1 で分割されたチャンクと、元の Markdown テキストを比較し、
表の構造が崩れている場合は補正し、行と列の対応関係を明確にした構造化データを生成してください。

**【重要】テーブルの各行、各セルに含まれる情報は、一切省略・要約しないでください。**
複数の行を「〜の明細一覧」などの一つの項目にまとめず、全ての行を個別に保持してください。

ページ情報:
{page_info}

入力データ:
{input_data}

出力形式はJSON配列のみとしてください。
"""

GRID_MAPPING_PROMPT = """あなたは構造化されたデータをExcelの方眼紙状の座標（行・列番号）にマッピングするアシスタントです。
1列=約2mm、1行=約5mmのグリッドを想定し、各要素がExcel上のどの範囲（start_row, start_col, end_row, end_col）に配置されるべきかを計算してください。

**【重要】A4縦の有効幅（約210mm）を考慮し、列1から列100程度までを最大限活用してください。**
特にメインの表（明細テーブル等）は、左右の余白を詰め、横幅いっぱいに広がるように（例: 列2から列105まで使用するなど）座標を割り当ててください。
中央に寄せて小さく配置せず、紙面を効果的に使うレイアウトを目指してください。

**【重要】テーブルデータは、各行がExcel上の独立した行（または複数の行に跨る範囲）に適切に割り振られるようにしてください。**
大きなテーブル全体を一つの大きな結合セルとして定義しないでください。各情報を読み取れるよう、行ごとに分割した配置情報を生成してください。

ページ情報:
{page_info}

入力データ:
{input_data}

出力形式はJSON配列のみとしてください。
"""

COMMAND_GENERATION_PROMPT = """あなたはExcel操作の指示を生成するアシスタントです。
STEP 3 で計算された座標に基づき、openpyxl 等で実行可能な描画コマンド（merge_and_set, set_borderなど）のリストを作成してください。
表構造に含まれるセルには `"border": true` を指定してください。

**【重要】入力データに含まれるテキスト内容は、一切省略したり要約したりせず、そのまま `"value"` に含めてください。**
「〜セクション」や「〜一覧」といったプレースホルダーに置き換えないでください。

ページ情報:
{page_info}

入力データ:
{input_data}

出力形式は以下のJSON配列のみとしてください:
[
  {{
    "page_number": 1,
    "value": [
      {{ "action": "merge_and_set", "start_row": 1, "start_column": 1, "end_row": 2, "end_column": 10, "value": "Title", "border": true }}
    ]
  }}
]
"""

PAGE_FIT_PROMPT = """あなたはExcelの印刷レイアウトを最適化するアシスタントです。
STEP 4 で生成されたコマンドを読み取り、コンテンツが A4 縦 1 ページ（または適切なページ数）の印字範囲内に綺麗に収まるよう、
必要に応じて行・列のインデックスをスケールダウン（再計算）して調整してください。

**【重要】スケーリングによる過度な縮小を防ぐため、以下の点に注意してください:**
1. データが存在する **最小の列・行から最大の列・行まで** を正確に計算し、不要な空白セルを含まないよう `print_range` を設定してください。
   （例：データがB列から始まっている場合、"A1"ではなく"B1"から印刷範囲を開始することを検討してください）
2. 全体のバランスを見て、文字が小さくなりすぎないよう、可能な限り広い領域を使って配置をスケーリングしてください。

**【重要】各アクションの `"value"` に含まれるテキスト内容は、レイアウト調整中も一切変更・省略しないでください。**

ページ情報:
{page_info}

入力データ:
{input_data}

出力形式は調整済みのJSON配列のみとしてください。
"""

EXCEL_CODE_GEN_PROMPT = """あなたは生成AIプログラミングのエキスパートです。
提供された Excel 描画コマンド（JSON）に基づき、`openpyxl` を用いて、
**A4 縦 1 ページ（または指定されたページ数）に完璧に収まる、非常に見栄えの良い Excel ファイルを作成する Python スクリプト**を生成してください。

【要件】
1. レイアウトの精密な再現:
   入力データの `start_row`, `start_column`, `end_row`, `end_column` を尊重しつつ、
   セルの高さや幅、フォントサイズなどを動的に微調整して、A4 の印字領域全体を無駄なく使い切ってください。

2. デザインの洗練:
   - 表のヘッダーは背景色（薄いグレーなど）を塗り、太字にする。
   - 罫線は、単一セルだけでなく、**結合セル（Merged Range）全体に適切に適用されるようループ等で処理する**。
   - 文字の折り返し (`wrap_text=True`) や垂直方向の中央揃え (`vertical='center'`) を適切に行う。

3. 印刷設定:
   - 用紙サイズ: A4
   - 向き: 縦
   - スケーリング: 「すべての列を1ページに印刷」
   - 印刷範囲の設定 (`ws.print_area`)

4. **重要: 技術的制約の遵守 (openpyxl)**:
   - **セルの値を設定してから `ws.merge_cells` を呼び出すこと**。あるいは、結合後に起点セル（左上）に対して値を設定すること。 `MergedCell` オブジェクトに対して値を設定しようとすると `AttributeError` になります。
   - 結合範囲が重複（overlap）しないように、入力データの重複をチェックするか、例外処理を入れること。

5. 出力ファイル:
   - `output.xlsx` という名前で保存するようにしてください。

ページ情報:
{page_info}

入力データ (STEP 5の出力):
{input_data}

出力は、**実行可能な Python コードのみ**をマークダウンのコードブロック (```python ... ```) で出力してください。
"""

ERROR_FIXING_PROMPT = """あなたはエラー解析とJSONデータの修正を行うアシスタントです。
以下のExcel描画コマンドをシステムで実行したところ、エラーが発生しました。
エラーメッセージを分析し、コンフリクト（結合範囲の重複）などを解消した正しいJSONを出力してください。

エラーログ:
{error_msg}

現在の入力データ:
{commands}

出力形式は修正されたJSON配列のみとしてください。
"""
