"""
Sheetling 用の LLM プロンプト定義集。
マルチステップ・パイプライン方式に基づき、各ステップの役割を定義します。
"""

CHUNKING_PROMPT = """あなたはPDFから抽出したデータを分析するアシスタントです。
入力データ（Markdownテキストと座標情報）を読み取り、論理的なセクションや表の塊（チャンク）ごとに分割してください。
各チャンクには、その種類（text, table, headingなど）と、大まかなページ内の位置情報を含めてください。

**【重要】 `content` には、その範囲に含まれる元のテキスト内容を一切省略・要約せずに、そのまま含めてください。**
テーブルの場合も「〜の一覧」のようにまとめず、Markdown 形式の表データをそのまま保持してください。

**【重要：結合禁止方針】同じ行（同じようなY座標）にあっても、左右に少しでも離れて配置されている別々の情報は、決して1つのテキストチャンクに結合しないでください。ユーザーが手作業で後から結合できるように、意味的な最小単位ごとに細かく分割して出力してください。**

**【重要】出力時のフォーマット指定**
出力フォーマットは以下の順のキーを持つJSON配列としてください。入力データの `pages` の中から該当ページの情報を引き継いでください。
[
  {{
    "page_number": 1,
    "width": 595.0,
    "height": 842.0,
    "data": [
      {{ "chunk_id": 1, "type": "...", "content": "..." }}
    ]
  }}
]

入力データ:
{input_data}
**【最重要】出力形式は指定された形式の純粋なJSON配列文字列のみとしてください。Markdownのコードブロック(```json等)や、前後の説明文、思考プロセスなどは一切含めないでください。**
"""

STRUCTURE_ALIGNMENT_PROMPT = """あなたは抽出されたテキストの論理構造を補正するアシスタントです。
STEP 1 で分割されたチャンクと、元の Markdown テキストを比較し、
表の構造が崩れている場合は補正し、行と列の対応関係を明確にした構造化データを生成してください。

**【最重要：Markdownテーブル構造の優先】MarkItDown から出力された Markdown テキストのテーブル構造（|---| 形式）を「絶対的な正解（Master Structure）」として扱ってください。Markdown 表内の各セルの並び順と内容を、JSON 構造の行・列として1対1で正確に対応させてください。**

**【重要】テーブルの各行、各セルに含まれる情報は、一切省略・要約しないでください。**
複数の行を「〜の明細一覧」などの一つの項目にまとめず、全ての行を個別に保持してください。

**【重要：結合禁止方針】同一行内であっても、空白などで左右に物理的に離れて配置されているテキストは、1つの文字列に結合せず、必ず分割して別々の項目（セルデータ）として保持してください。自動的なセル結合は一切行わず、レイアウトの維持を最優先してください。**

**【重要】出力時のフォーマット指定**
入力データで渡されたJSON配列の構造（`page_number`, `width`, `height`）はそのまま維持し、`data` 配列の中身だけを構造補正した内容に置き換えて出力してください。

入力データ:
{input_data}
**【最重要】出力形式は指定された形式の純粋なJSON配列文字列のみとしてください。Markdownのコードブロック(```json等)や、前後の説明文、思考プロセスなどは一切含めないでください。**
"""

GRID_SIZES = {
    "small": {
        "col_width_mm": "4.0",
        "row_height_mm": "4.0",
        "max_cols": 62,
        "max_rows": 76,
        "excel_col_width": 1.45,
        "excel_row_height": 11.34,
        "margin_left": 0.43,
        "margin_right": 0.43,
        "margin_top": 0.41,
        "margin_bottom": 0.41
    },
    "medium": {
        "col_width_mm": "6.0",
        "row_height_mm": "6.0",
        "max_cols": 36,
        "max_rows": 50,
        "excel_col_width": 2.53,
        "excel_row_height": 17.01,
        "margin_left": 0.47,
        "margin_right": 0.47,
        "margin_top": 0.41,
        "margin_bottom": 0.41
    },
    "large": {
        "col_width_mm": "8.0",
        "row_height_mm": "8.0",
        "max_cols": 26,
        "max_rows": 38,
        "excel_col_width": 3.61,
        "excel_row_height": 22.68,
        "margin_left": 0.51,
        "margin_right": 0.51,
        "margin_top": 0.49,
        "margin_bottom": 0.49
    }
}

GRID_MAPPING_PROMPT = """あなたは構造化されたデータをExcelの方眼紙状の座標（行・列番号）にマッピングするアシスタントです。
「1列=約{col_width_mm}mm」「1行=約{row_height_mm}mm」のグリッドを想定し、各要素がExcel上のどの範囲（start_row, start_col, end_row, end_col）に配置されるべきかを計算してください。
**【重要】A4縦の有効印字幅・高さに対して、列1から最大「列{max_cols}」、行1から最大「行{max_rows}」の範囲内に完全に収まるようにアスペクト比を維持したまま配置してください。**
・用紙サイズ: A4縦 (210mm × 297mm の絶対アスペクト比)
・方眼サイズと座標系設定:
    - 1マス = 今回のグリッドは {col_width_mm}mm 四方の正方形です
    - 方眼紙の列数 (幅): 左から右へ 1列目 〜 {max_cols}列目
    - 方眼紙の行数 (高さ): 上から下へ 1行目 〜 {max_rows}行目
    - **【重要指示】Excelの印刷設定（縮小して全体を印刷など）には一切頼りません。** A4サイズの紙面内に指定された限界値（列・行数）へ等倍で収まるよう、数学的に方眼サイズ・余白が固定されています。
    - したがって、入力となるPDF元データから、この絶対座標系（最大 {max_cols}列 × {max_rows}行）へ、**あなた自身がアスペクト比を保ちつつ比例配分（アフィン変換）してマッピングを調整**してください。
    - PDFが非常に複雑な場合でも、決して限界列数・行数をはみ出さないようにこの座標内に収めるのがあなたの役割です。

特にメインの表（明細テーブル等）は、左右の余白を詰め、全体の横幅いっぱいに広がるように（例: 列2から列{max_cols}まで使用するなど）座標を割り当ててください。
中央に寄せて小さく配置せず、指定された列数・行数をフルに使ったレイアウトを目指してください。

**【重要】同一行内で左右に離れて配置された異なる情報（例：左側に「会社名」、右側に「日付」など）は、実際の紙面レイアウトに合わせて左右の別々の列に配置されるよう、独立した要素としてマッピングしてください。**

**【重要】出力時のフォーマット指定**
入力データで渡されたJSON配列の構造（`page_number`, `width`, `height`）はそのまま維持し、`data` 配列の中身だけを座標計算した内容に置き換えて出力してください。

入力データ:
{input_data}
**【最重要】出力形式は指定された形式の純粋なJSON配列文字列のみとしてください。Markdownのコードブロック(```json等)や、前後の説明文、思考プロセスなどは一切含めないでください。**
"""

COMMAND_GENERATION_PROMPT = """あなたはExcel操作の指示を生成するアシスタントです。
STEP 3 で計算された座標に基づき、openpyxl 等で実行可能な描画コマンド（set_value, set_borderなど）のリストを作成してください。
**【重要：範囲情報と結合の完全禁止】表の枠線を正しく描画するため、各要素には必ず `start_row`, `start_column` から `end_row`, `end_column` までの範囲情報を含めてください。ただし、`merge_and_set` などのセル結合を伴うアクションは一切使用せず、すべて `set_value` 形式で出力してください。**
表構造に含まれるセルには `"border": true` を指定してください。

**【重要】入力データに含まれるテキスト内容は、一切省略したり要約したりせず、そのまま `"value"` に含めてください。**
「〜セクション」や「〜一覧」といったプレースホルダーに置き換えないでください。

**【重要】出力時のフォーマット指定**
入力データで渡されたJSON配列の構造（`page_number`, `width`, `height`）はそのまま維持し、`data` 配列の中身だけをコマンドリストの内容に置き換えて出力してください。

入力データ:
{input_data}

**【最重要】出力形式は以下の形式の純粋なJSON配列文字列のみとしてください。Markdownのコードブロック(```json等)や、前後の説明文、思考プロセスなどは一切含めないでください:**
[
  {{
    "page_number": 1,
    "width": 595.0,
    "height": 842.0,
    "data": [
      {{ "action": "set_value", "start_row": 1, "start_column": 1, "end_row": 2, "end_column": 10, "value": "Title", "border": true }}
    ]
  }}
]
"""

PAGE_FIT_PROMPT = """あなたはExcelの印刷レイアウトを厳密に検証し調整するアシスタントです。
STEP 4 で生成されたコマンドを読み取り、全体のレイアウトが「列{max_cols}制限」というA4物理幅（約180mm）を超過していないかをチェックし、
必要に応じてインデックスを再計算して調整してください。

**【重要】全体座標の最終調整ルール:**
1. 入力されたコマンド群の中で、`end_column` の最大値が「{max_cols}」を超える、または `end_row` の最大値が「{max_rows}」を超える場合、すべての座標の比率を保ったまま、最大列が{max_cols}以内かつ最大行が{max_rows}以内に収まるように全体を縮小・再計算を行ってください。
2. データが存在する **最小の列・行から最大の列・行まで** を正確に計算し、不要な右側の空白セルを含まないよう `print_range` （例："B1:CV50"など）を設定してください。
   （例：データがB列から始まっている場合、"A1"ではなく"B1"から印刷範囲を開始することを検討してください）

**【重要】各アクションの `"value"` に含まれるテキスト内容は、レイアウト調整中も一切変更・省略しないでください。**

**【重要】出力時のフォーマット指定**
入力データで渡された構造を維持しつつ、必要に応じて全体に対する `print_range` などをページレベルのキーとして追加し、`data` には調整済みのコマンドを格納してください。

入力データ:
{input_data}
**【最重要】出力形式は指定された形式の純粋なJSON配列文字列のみとしてください。Markdownのコードブロック(```json等)や、前後の説明文、思考プロセスなどは一切含めないでください。**
"""

EXCEL_CODE_GEN_PROMPT = """あなたは生成AIプログラミングのエキスパートです。
提供された Excel 描画コマンド（JSON）に基づき、`openpyxl` を用いて、
**A4 縦 1 ページ（または指定されたページ数）の実寸大で完璧に収まる、非常に見栄えの良い Excel ファイルを作成する Python スクリプト**を生成してください。

【要件】
1. レイアウトと方眼紙サイズの厳密な設定:
   入力データの座標は、すでにA4横幅（最大約{max_cols}列）にフィットするように計算されています。
   以下の数値を列幅と行の高さとして設定するコードを必ず含めてください。
   - `ws.column_dimensions[...].width = {excel_col_width}` （1列＝約{col_width_mm}mm）
   - `ws.row_dimensions[...].height = {excel_row_height}` （1行＝約{row_height_mm}mm）
   **重要: 複数ページの場合に備え、行の高さの設定は `1` 行目から「最大ページ数 × {max_rows}」行目程度まで適用するループを作成してください。**

2. デザインの洗練:
   - 表のヘッダーは背景色（薄いグレーなど）を塗り、太字にする。
   - 罫線は、単一セルに適切に適用されるようループ等で処理する。
   - **重要：セルの結合を行わない方針（Zero-Merge）に対応するため、すべてのテキストセルに対して「折り返して全体を表示」 (`wrap_text=True`) と「垂直方向の中央揃え」 (`vertical='center'`) をデフォルトで適用し、テキストが隣のセルに隠れないように配慮する。**

3. 印刷設定（縮小機能に一切頼らない絶対等倍）:
   - 用紙サイズ: A4 (openpyxlの仕様に合わせ、ws.page_setup.paperSize には定数などを使わずに直接 9 を代入してください)
   - 向き: 縦 (ws.page_setup.orientation には定数などを使わずに直接 'portrait' を代入してください)
   - **重要: スケーリング（`fitToWidth` や `fitToPage`）の自動縮小設定は無効化するか、一切記述しないでください。縮小不要な等倍（100%）で出力されることが大前提です。**
   - 印刷範囲の設定 (`ws.print_area`) は、入力データに `print_range` があればそれを使用してください。
   - **重要: ページ中央に配置し、真のA4サイズと一致する数学的余白として、必ず以下のコードを含めてください。**
     ```python
     ws.print_options.horizontalCentered = True
     ws.page_margins.left = {margin_left}
     ws.page_margins.right = {margin_right}
     ws.page_margins.top = {margin_top}
     ws.page_margins.bottom = {margin_bottom}
     ```
4. 複数ページへの完全対応:
   - 入力JSONに複数ページのデータが含まれる場合、**シートをページごとに分けず、必ず「1つの同じワークシート(`ws = wb.active`)」に全ページのデータを縦に並べて出力**してください。
   - `page_number` をキーとし、2ページ目以降のデータには **「(page_number - 1) * {max_rows}」分の行オフセット**を算出し、各アイテムの `s_row` と `e_row` に加算して下にずらすロジックを必ず実装してください。これによりA4単位で綺麗に行が配置されます。
   - ページ間の境界には改ページ設定 (`from openpyxl.worksheet.pagebreak import Break; ws.row_breaks.append(Break(id=ページ単位の行オフセット))`) を行い、印刷時にA4単位で正しく分割されるようにしてください。
   - 最終的な印刷範囲 (`ws.print_area`) は、全ページを含んだ全体のセル範囲を指定してください。
5. **重要: 技術的制約の遵守 (openpyxl)**:
   - セルの値を設定する際は、以下の様に `try...except AttributeError:` のブロックで囲んでエラーをスキップし、処理が途中で落ちないようにしてください。
   - **【最重要】セルの結合（ws.merge_cells）を行うコードは一切生成しないでください。結合は厳禁です。**
   - 代わりに、入力JSONに含まれる `start_row, start_column` から `end_row, end_column` までの全範囲に対して枠線を設定するループコードを生成してください。
     ```python
     try:
         # 値の設定は左上のセルのみに行う
         cell = ws.cell(row=s_row, column=s_col)
         cell.value = val
         # 範囲全体(s_row, s_col から e_row, e_col)に対して二重ループで枠線や折り返し設定を適用する
         for r in range(s_row, e_row + 1):
             for c in range(s_col, e_col + 1):
                 target_cell = ws.cell(row=r, column=c)
                 # 枠線設定や alignment(wrap_text=True) 等の処理をここに記述
     except AttributeError:
         pass
     ```

6. 出力ファイル:
   - `output.xlsx` という名前で保存するようにしてください。

入力データ (STEP 5の出力):
{input_data}
**【最重要】出力は、実行可能な Python コードのみをマークダウンのコードブロック (```python ... ```) で出力してください。前後の挨拶、説明文、思考プロセスなどは一切不要です。**
**【最重要】出力するコード内には、`[cite: ...]` のような参照タグやアノテーション（例: ` [cite: 271]`）を絶対に含めないでください。SyntaxErrorの原因となります。純粋で実行可能なPythonコードのみを出力してください。また、コードブロックの外にいかなるテキストも記述しないでください。**
"""

ERROR_FIXING_PROMPT = """あなたはエラー解析とJSONデータの修正を行うアシスタントです。
以下のExcel描画コマンドをシステムで実行したところ、エラーが発生しました。
エラーメッセージを分析し、コンフリクト（結合範囲の重複）などを解消した正しいJSONを出力してください。

エラーログ:
{error_msg}

現在の入力データ:
{commands}
**【最重要】出力形式は修正された純粋なJSON配列文字列のみとしてください。Markdownのコードブロック(```json等)や、前後の説明文、思考プロセスなどは一切含めないでください。**
"""

CODE_ERROR_FIXING_PROMPT = """あなたはPythonプログラミングのエキスパートです。
以下のExcel生成用Pythonスクリプトを実行したところ、エラーが発生しました（または期待する結果が得られませんでした）。
エラーメッセージと現在のコードを分析し、問題を修正した新しいPythonスクリプトを生成してください。

【エラーメッセージ・実行結果】
{error_msg}

【現在のコード】
```python
{code}
```
**【最重要】出力は、修正済みの実行可能な Python コードのみをマークダウンのコードブロック (```python ... ```) で出力してください。前後の挨拶、解説、謝罪、思考プロセスなどは一切不要です。**
**【最重要】出力するコード内には、`[cite: ...]` のような参照タグやアノテーション（例: ` [cite: 271]`）を絶対に含めないでください。SyntaxErrorの原因となります。純粋で実行可能なPythonコードのみを出力してください。また、コードブロックの外にいかなるテキストも記述しないでください。**
**【重要】`AttributeError: 'MergedCell' object attribute 'value' is read-only` を回避するため、セルへの値の代入や `merge_cells` 等の処理は、必ず `try...except AttributeError:` で囲み、重複座標エラーを握り潰す（passする）ように修正してください。**
"""
