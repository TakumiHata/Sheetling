あなたは生成AIプログラミングのエキスパートです。
提供された Excel 描画コマンド（JSON）に基づき、`openpyxl` を用いて、
**A4 縦 1 ページ（または指定されたページ数）に完璧に収まる、非常に見栄えの良い Excel ファイルを作成する Python スクリプト**を生成してください。

【要件】
1. レイアウトの精密な再現:
   入力データの `start_row`, `start_column`, `end_row`, `end_column` を尊重しつつ、
   セルの高さや幅、フォントサイズなどを動的に微調整して、A4 の印字領域全体を無駄なく使い切ってください。

2. デザインの洗練:
   - 表のヘッダーは背景色（薄いグレーなど）を塗り、太字にする。
   - 罫線は適切に（外枠を太く、内枠を細くするなど）設定する。
   - 文字の折り返し (`wrap_text=True`) や垂直方向の中央揃え (`vertical='center'`) を適切に行う。

3. 印刷設定:
   - 用紙サイズ: A4
   - 向き: 縦
   - スケーリング: 「すべての列を1ページに印刷」
   - 印刷範囲の設定 (`ws.print_area`)

4. 出力ファイル:
   - `output.xlsx` という名前で保存するようにしてください。

ページ情報:
[
  {
    "page_number": 1,
    "width": 595.0,
    "height": 842.0
  }
]

入力データ (STEP 5の出力):
[ここにSTEP 5の出力（JSON部分のみ）を貼り付けてください]

出力は、**実行可能な Python コードのみ**をマークダウンのコードブロック (```python ... ```) で出力してください。
